version: '3'

services:
  flaskapp:
    build: ./flaskapp-docker
    container_name: flaskapp
    ports:
      - "12000:12000"
#    network_mode: host

  nginx:
    build: ./nginx
    container_name: nginx
    ports:
      - "12001:12001"
    environment:
      - SERVER_NAME=34.240.222.177
    depends_on:
      - flaskapp
    restart: always
#    network_mode: host